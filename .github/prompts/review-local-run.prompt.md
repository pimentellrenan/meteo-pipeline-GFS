# Review Local Run

Review the repository for local reproducibility.

Check:

- whether `README.md` matches the CLI;
- whether `.env.example` covers required settings;
- whether generated GRIB2, PNG, CSV, and cache files are ignored;
- whether agent instructions remain aligned across `AGENTS.md`, `CLAUDE.md`, `.claude/`, and `.github/`.

